import React from 'react';
import { View } from 'react-native';
import { HomeScreen } from './src/screens/HomeScreen';

const app: React.FC = () => {
  return (
    <View>
      <HomeScreen />
    </View>
  );
};

export default app;
